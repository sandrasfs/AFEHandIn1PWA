import { ShortYearFromFullYearPipe } from './short-year-from-full-year.pipe';

describe('ShortYearFromFullYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortYearFromFullYearPipe();
    expect(pipe).toBeTruthy();
  });
});
